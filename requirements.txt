# FastAPI and ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Existing crypto-listener dependencies
python-dotenv
python-binance
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9,<3.0
requests>=2.0
redis

# Note: logging and RotatingFileHandler are part of Python stdlib (no install needed)
